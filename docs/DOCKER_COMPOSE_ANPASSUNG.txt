╔════════════════════════════════════════════════════════════════════════════════╗
║                    DOCKER-COMPOSE.YML - EXAKTE ÄNDERUNGEN                      ║
╚════════════════════════════════════════════════════════════════════════════════╝

HIER IST DIE KOMPLETTE, FERTIGE docker-compose.yml:

─────────────────────────────────────────────────────────────────────────────────

version: '3.8'

services:
  loxoneaudioserver:
    container_name: lox-audioserver
    image: ghcr.io/rudyberends/lox-audioserver:latest
    hostname: lox-audioserver
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: unless-stopped
    network_mode: host
    
    # Capabilities für SMB Mounts
    cap_add:
      - SYS_ADMIN
      - DAC_READ_SEARCH
    
    # ⭐ NEUE ZEILEN: USB Devices
    devices:
      - /dev/snd:/dev/snd              # Soundkarte (Audio)
      - /dev/ttyUSB0:/dev/ttyUSB0      # USB Relais (ARCELI)
    
    # ⭐ NEUE ZEILEN: PowerManager Umgebungsvariablen
    environment:
      # PowerManager Einstellungen
      PM_ENABLED: "true"
      PM_USB_PORT: "/dev/ttyUSB0"
      PM_USB_BAUD_RATE: "9600"
      PM_CHANNEL: "1"
      PM_TURN_ON_AT_PLAY: "true"
      PM_TURN_OFF_DELAY: "5"
    
    # Persistente Daten
    volumes:
      - ./data:/app/data

─────────────────────────────────────────────────────────────────────────────────

╔════════════════════════════════════════════════════════════════════════════════╗
║                              WAS WURDE GEÄNDERT?                               ║
╚════════════════════════════════════════════════════════════════════════════════╝

✅ HINZUGEFÜGT: Devices Sektion
┌────────────────────────────────────────────────────────────────────────────┐
│ devices:                                                                    │
│   - /dev/snd:/dev/snd              # SOUNDKARTE                             │
│   - /dev/ttyUSB0:/dev/ttyUSB0      # USB RELAIS                            │
└────────────────────────────────────────────────────────────────────────────┘

✅ HINZUGEFÜGT: PowerManager Umgebungsvariablen
┌────────────────────────────────────────────────────────────────────────────┐
│ environment:                                                                │
│   PM_ENABLED: "true"              # Schalter: true/false                   │
│   PM_USB_PORT: "/dev/ttyUSB0"     # USB Port (ggf. ttyUSB1, etc.)         │
│   PM_USB_BAUD_RATE: "9600"        # Serieller Speed (nicht ändern!)        │
│   PM_CHANNEL: "1"                 # Relais Kanal 1-4                       │
│   PM_TURN_ON_AT_PLAY: "true"      # Auto-ON bei Musik (true/false)        │
│   PM_TURN_OFF_DELAY: "5"          # Verzögerung in Sekunden                │
└────────────────────────────────────────────────────────────────────────────┘

─────────────────────────────────────────────────────────────────────────────────

╔════════════════════════════════════════════════════════════════════════════════╗
║                         ANPASSUNG FÜR IHR SETUP                                ║
╚════════════════════════════════════════════════════════════════════════════════╝

SCHRITT 1: USB Port überprüfen
──────────────────────────────

Bevor Sie die Datei anpassen, schauen Sie welcher Port Ihr Relais hat:

  Linux/NAS/Raspberry Pi:
  $ ls -la /dev/ttyUSB*
  
  Typische Ausgabe:
  /dev/ttyUSB0  ← Das ist Ihr Relais (Standard)
  /dev/ttyUSB1  ← Falls mehrere USB-Geräte, dann könnte es ttyUSB1 sein
  
  Windows (falls Sie Windows Server nutzen):
  COM3, COM4, etc. (aus Device Manager)

Falls Sie /dev/ttyUSB0 sehen:
  ➜ Sie können docker-compose.yml direkt verwenden (keine Änderung nötig!)

Falls Sie /dev/ttyUSB1 sehen:
  ➜ Ändern Sie diese Zeile:
     PM_USB_PORT: "/dev/ttyUSB1"   ← Statt ttyUSB0
     devices:
       - /dev/ttyUSB1:/dev/ttyUSB1 ← Statt ttyUSB0

─────────────────────────────────────────────────────────────────────────────────

SCHRITT 2: docker-compose.yml speichern
─────────────────────────────────────────

Die Datei befindet sich im Hauptverzeichnis von lox-audioserver-beta:

  📁 lox-audioserver-beta/
     ├── docker-compose.yml   ← Diese Datei!
     ├── src/
     ├── package.json
     └── ...

Editieren Sie die Datei mit einem Text-Editor (VS Code, nano, vi, etc.):

  VS Code:
  $ code docker-compose.yml

  nano:
  $ nano docker-compose.yml

  vim:
  $ vim docker-compose.yml

─────────────────────────────────────────────────────────────────────────────────

SCHRITT 3: Inhalte exakt kopieren
───────────────────────────────────

Öffnen Sie die Datei und ersetzen Sie komplett mit:

  version: '3.8'

  services:
    loxoneaudioserver:
      container_name: lox-audioserver
      image: ghcr.io/rudyberends/lox-audioserver:latest
      hostname: lox-audioserver
      build:
        context: .
        dockerfile: ./Dockerfile
      restart: unless-stopped
      network_mode: host
      
      cap_add:
        - SYS_ADMIN
        - DAC_READ_SEARCH
      
      # ⭐ GERÄTE
      devices:
        - /dev/snd:/dev/snd
        - /dev/ttyUSB0:/dev/ttyUSB0
      
      # ⭐ POWERMANAGER
      environment:
        PM_ENABLED: "true"
        PM_USB_PORT: "/dev/ttyUSB0"
        PM_USB_BAUD_RATE: "9600"
        PM_CHANNEL: "1"
        PM_TURN_ON_AT_PLAY: "true"
        PM_TURN_OFF_DELAY: "5"
      
      volumes:
        - ./data:/app/data

─────────────────────────────────────────────────────────────────────────────────

SCHRITT 4: Datei speichern & Container starten
────────────────────────────────────────────────

Speichern Sie die Datei (Ctrl+S in VS Code, Esc + :wq in vim)

Dann im Terminal im lox-audioserver-beta Verzeichnis:

  $ npm install              # Dependencies installieren
  $ npm run build            # Project bauen
  $ docker-compose up -d     # Container starten
  $ docker-compose logs -f   # Logs verfolgen

Erfolgreiche Logs sehen so aus:
  [PowerManagement] USB Relais connected (port=/dev/ttyUSB0, baudRate=9600, channel=1)

─────────────────────────────────────────────────────────────────────────────────

╔════════════════════════════════════════════════════════════════════════════════╗
║                            SPEZIELLE SZENARIEN                                 ║
╚════════════════════════════════════════════════════════════════════════════════╝

SZENARIO 1: Relais auf USB Port 1 (statt 0)
─────────────────────────────────────────────

Ändern Sie diese zwei Zeilen:

  devices:
    - /dev/snd:/dev/snd
    - /dev/ttyUSB1:/dev/ttyUSB1      ← Von ttyUSB0 zu ttyUSB1

  environment:
    PM_USB_PORT: "/dev/ttyUSB1"      ← Von /dev/ttyUSB0 zu /dev/ttyUSB1

─────────────────────────────────────────────────────────────────────────────────

SZENARIO 2: PowerManager deaktivieren (nur Audioserver ohne Relais)
──────────────────────────────────────────────────────────────────────

Ändern Sie diese Zeile:

  environment:
    PM_ENABLED: "false"              ← Von "true" zu "false"

Alle anderen PM_* Variablen können dann ignoriert werden.

─────────────────────────────────────────────────────────────────────────────────

SZENARIO 3: Relais schneller ausschalten (2 Sekunden statt 5)
──────────────────────────────────────────────────────────────

Falls Sie zu viel Klicks hören:

  environment:
    PM_TURN_OFF_DELAY: "2"           ← Von 5 zu 2 Sekunden

─────────────────────────────────────────────────────────────────────────────────

SZENARIO 4: Relais wird sofort ausgeschaltet (keine Verzögerung)
─────────────────────────────────────────────────────────────────

  environment:
    PM_TURN_OFF_DELAY: "0"           ← Von 5 zu 0 Sekunden

⚠️ Warnung: Das könnte zu Klicks führen!

─────────────────────────────────────────────────────────────────────────────────

╔════════════════════════════════════════════════════════════════════════════════╗
║                              HÄUFIGE FEHLER                                    ║
╚════════════════════════════════════════════════════════════════════════════════╝

❌ FEHLER 1: Falsche Einrückung (YAML Spaces!)
──────────────────────────────────────────────

FALSCH:
  devices:
  - /dev/snd:/dev/snd        ← Keine Einrückung!

RICHTIG:
  devices:
    - /dev/snd:/dev/snd      ← 2 Spaces Einrückung!

YAML ist SEHR empfindlich auf Spaces! Verwenden Sie einen YAML Editor oder VS Code.

─────────────────────────────────────────────────────────────────────────────────

❌ FEHLER 2: Falsche Port-Angabe
────────────────────────────────

FALSCH:
  PM_USB_PORT: "ttyUSB0"          ← Fehlt /dev/!
  PM_USB_PORT: "/dev/ttyUSB0/"    ← Trailing Slash!
  PM_USB_PORT: "COM3"             ← Linux braucht /dev/!

RICHTIG:
  PM_USB_PORT: "/dev/ttyUSB0"     ← Exakt so!

─────────────────────────────────────────────────────────────────────────────────

❌ FEHLER 3: Boolean statt String
─────────────────────────────────

FALSCH:
  PM_ENABLED: true               ← Ohne Anführungszeichen!
  PM_TURN_ON_AT_PLAY: false      ← Boolean!

RICHTIG:
  PM_ENABLED: "true"             ← Mit Anführungszeichen!
  PM_TURN_ON_AT_PLAY: "true"    ← Als String!

─────────────────────────────────────────────────────────────────────────────────

❌ FEHLER 4: Negative Zahlen bei Baud Rate
────────────────────────────────────────────

FALSCH:
  PM_USB_BAUD_RATE: 9600         ← Keine Anführungszeichen (wird zu Integer)

RICHTIG:
  PM_USB_BAUD_RATE: "9600"       ← Mit Anführungszeichen!

─────────────────────────────────────────────────────────────────────────────────

╔════════════════════════════════════════════════════════════════════════════════╗
║                             VALIDIERUNG                                        ║
╚════════════════════════════════════════════════════════════════════════════════╝

Um YAML Syntax zu überprüfen:

  Online Validator:
  https://www.yamllint.com/

  Oder im Terminal:
  $ docker run -v $(pwd):/workspace ghcr.io/adrienverge/yamllint docker-compose.yml

Falls Fehler:
  "mapping values are not allowed here" → Einrückung prüfen!
  "found character '@' that cannot start any token" → Sonderzeichen?

─────────────────────────────────────────────────────────────────────────────────

✅ WENN ALLES OK IST:

Speichern, Terminal öffnen, Command eingeben:

  cd /path/to/lox-audioserver-beta
  docker-compose up -d

Logs sehen:
  docker-compose logs -f

FERTIG! 🎉
